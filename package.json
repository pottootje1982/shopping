{
  "name": "shopping",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "jest --verbose ./api"
  },
  "pre-commit": [
    "test"
  ],
  "devDependencies": {
    "jest": "^24.9.0",
    "pre-commit": "^1.2.2"
  }
}
