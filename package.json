{
  "name": "shopping",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "jest --verbose ./api",
    "start": "pm2-runtime start ecosystem.config.js --env production"
  },
  "pre-commit": [
    "test"
  ],
  "devDependencies": {
    "pre-commit": "^1.2.2"
  },
  "dependencies": {
    "dotenv": "^8.2.0"
  }
}
